version: '3.8'

services:

  # ── Nginx — serves all HTML + proxies to API ──────────────
  web:
    image: nginx:alpine
    ports:
      - "8080:8080"
    volumes:
      - .:/var/www/whoamisec:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - apk_data:/var/apk:ro
    depends_on:
      - api
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/health"]
      interval: 30s
      retries: 3

  # ── Flask API — auth, subscriptions, VPS mgmt, AI ─────────
  api:
    build: .
    ports:
      - "5001:5001"
    volumes:
      - ./backend:/app
      - api_data:/app/data
    environment:
      - SECRET_KEY=${SECRET_KEY:-whoamisec-secret-change-me}
      - JWT_SECRET_KEY=${JWT_SECRET_KEY:-whoamisec-jwt-change-me}
      - OLLAMA_HOST=http://host.docker.internal:11434
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5001/api/v1/health"]
      interval: 30s
      retries: 3

  # ── Cloudflare Tunnel — whoamisec.com → this Codespace ────
  # FREE — no VPS rental, no server costs, no paid hosting
  # Setup: see tunnel-setup.sh
  tunnel:
    image: cloudflare/cloudflared:latest
    command: tunnel --no-autoupdate run --token ${CF_TUNNEL_TOKEN}
    environment:
      - TUNNEL_TOKEN=${CF_TUNNEL_TOKEN}
    depends_on:
      - web
    restart: unless-stopped
    profiles:
      - tunnel   # only starts with: docker compose --profile tunnel up

  # ── CICADA Anti-AI Encryption Engine (port 5110) ──────────
  cicada-anti-ai:
    build: ./cicada-anti-ai
    ports:
      - "5110:5110"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5110/api/health"]
      interval: 30s
      retries: 3

  # ── CICADA Puzzle Gate Basic (port 5115) ───────────────────
  cicada-puzzle-gate:
    build: ./cicada-anti-ai
    command: python cicada_puzzle_gate.py
    ports:
      - "5115:5115"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5115/api/health"]
      interval: 30s
      retries: 3

  # ── CICADA Ultimate Gate — 12 puzzle types (port 5120) ────
  cicada-ultimate-gate:
    build: ./cicada-anti-ai
    command: python cicada_ultimate_puzzle.py
    ports:
      - "5120:5120"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5120/api/health"]
      interval: 30s
      retries: 3

  # ── CICADA Payment Gateway — 7 methods (port 5130) ────────
  cicada-payment:
    build: ./cicada-anti-ai
    command: python cicada_payment_gateway.py
    ports:
      - "5130:5130"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5130/api/health"]
      interval: 30s
      retries: 3

  # ── APK watchdog — auto-downloads new APK releases ────────
  apk-watchdog:
    image: alpine:3.19
    volumes:
      - apk_data:/var/apk
      - ./apk-watchdog.sh:/watchdog.sh:ro
    environment:
      - GH_APK_WATCHDOG_TOKEN=${GH_APK_WATCHDOG_TOKEN:-}
      - APK_DIR=/var/apk
    command: sh /watchdog.sh
    restart: unless-stopped
    profiles:
      - apk    # only starts with: docker compose --profile apk up

volumes:
  api_data:
  apk_data:
